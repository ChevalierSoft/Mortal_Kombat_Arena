
#ifndef DEF_JEU
#define DEF_JEU

  #include "fonctions.h"

    int jouer(SDL_Renderer* renderer,personnage_t * personnage,carte_t * pt_m);
    void creer_map(SDL_Renderer * renderer);



#endif
